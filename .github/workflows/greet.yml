name: First CI Workflow
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v4
      - name: Print Message
        run: echo "Hello world"
     
     
     
